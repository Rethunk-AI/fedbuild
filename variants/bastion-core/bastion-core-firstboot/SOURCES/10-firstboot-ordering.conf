# Drop-in for bastion-core.service installed by bastion-core-firstboot RPM.
# Ensures the C2 server starts only after firstboot has rolled the PKI and
# written /var/lib/bastion/install/bootstrap.env with the per-VM SAI.
[Unit]
After=bastion-core-firstboot.service
Wants=bastion-core-firstboot.service
