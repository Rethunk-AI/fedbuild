# Drop-in for bastion-qemu.service installed by bastion-core-firstboot RPM.
# Ensures bastion-qemu starts only after firstboot has provisioned
# /var/lib/bastion/service-ca/ and /var/lib/bastion/qemu (ReadWritePaths).
[Unit]
After=bastion-core-firstboot.service
Wants=bastion-core-firstboot.service
